import styled from "styled-components";

export const Container = styled.h1`
  color: #ff9000;
  font-size: 3rem;
  text-shadow: -1px 0 var(--gray-700), 0 1px var(--gray-700),
    1px 0 var(--gray-700), 0 -1px var(--gray-700);
  padding: 0 0.3rem;
  border-radius: 5px;
  span {
    color: #002243;
  }
`;
