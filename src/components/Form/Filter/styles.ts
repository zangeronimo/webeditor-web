import styled from 'styled-components';

export const Container = styled.div`
  padding: 0.5rem 0;
  form {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  > button {
    border: none;
    background: transparent;
    font-size: 0.75rem;
    margin-top: 0.5rem;
  }

  hr {
    margin-top: 1rem;
  }
`;
