import styled from 'styled-components';

export const Container = styled.div``;

export const ShowModal = styled.span`
  cursor: pointer;
`;

export const Content = styled.div`
  padding: 1rem;
  margin: 0;
`;

export const Title = styled.div`
  background: var(--blue-500);
  color: var(--gray-100);
`;
